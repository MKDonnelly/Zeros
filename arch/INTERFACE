Everything that is arch-specific but not prefixed with 
arch_ is never used outside of the arch code. 
Each arch system exposes the following interfaces 
grouped by purpose.

Timer interface:
   The arch timer interface serves one basic
   purpose: run a callback function on every
   timer tick.

   //Initilizes timer subsystem
   arch_init_timer()

Interrupts:

Memory descriptors:

CPU functions:
   arch_stop_cpu
   

